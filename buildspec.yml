version: 0.2

phases:
  install:
    runtime-versions:
      dotnet: 6.0
  build:
    commands:
      - cd Todolendar.API
      - dotnet restore
      - dotnet build -c Release
      - dotnet publish -c Release -o ./publish
artifacts:
  files:
    - ./Todolendar.API/publish/**/*